<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

<item android:id="@android:id/background">
    <shape>
        <corners android:radius="5dip" />
        <gradient
                android:startColor="@color/number_color"
                android:centerColor="@color/number_color"
                android:endColor="@color/number_color"
        />
    </shape>
</item>
<item
    android:id="@android:id/progress">
    <clip>
        <shape>
            <corners
                android:radius="5dip" />
            <gradient
                android:startColor="@color/negative"
                android:centerColor="@color/negative"
                android:endColor="@color/negative"
        />
        </shape>
    </clip>
</item>
</layer-list>